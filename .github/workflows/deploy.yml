name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master # Or your default branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    # Grant GITHUB_TOKEN permissions to write to the gh-pages branch
    permissions:
      contents: read
      pages: write
      id-token: write
      
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22' # Match your development environment
          cache: 'npm'
          cache-dependency-path: microscope-web/package-lock.json

      - name: Install Dependencies 📦
        run: npm install
        working-directory: ./microscope-web

      - name: Build 🏗️
        run: npm run build
        working-directory: ./microscope-web

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './microscope-web/dist'

      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4
